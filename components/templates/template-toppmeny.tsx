/**
 * COLLECTIUM FILE HEADER
 *
 * Overskrift:
 * template-toppmeny
 *
 * Definering / formål:
 * Eneste godkjente templatefil for global toppmeny.
 *
 * Bruksområde:
 * Global template / senere AppShell.
 *
 * Berørte sider / routes:
 * - alle sider via global template når aktivert
 *
 * Berørte DB-brytere / feature_keys:
 * - local.template.toppmeny
 *
 * Berørte API-ruter:
 * - senere /api/admin/menu eller DB-menykilde
 *
 * Berørte tabeller / views:
 * - senere ct_v_app_menu
 *
 * Dataretning:
 * MariaDB/API senere → Template → React → UI
 *
 * Logging:
 * ingen
 *
 * Versjon:
 * CT-FILE-TEMPLATE-TOPPMENY-0001 / CHANGE-2026-06-06-STRUCTURE-LOCK
 */

export function TemplateToppmeny() {
  return null;
}